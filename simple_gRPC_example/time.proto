syntax = "proto3";
package time;

service Time {
    rpc GetTime (TimeRequest) returns (TimeReply) {}
}

//Empty Request Message
message TimeRequest {
}

//The Response message containing time
message TimeReply {
    string message = 1;
}